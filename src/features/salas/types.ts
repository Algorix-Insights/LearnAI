export type RoomHighlightCard = {
  id: string;
  title: string;
  label: string;
  createdText: string;
  detail: string;
};

export type RoomItem = {
  id: string;
  title: string;
  detail: string;
  createdText: string;
};